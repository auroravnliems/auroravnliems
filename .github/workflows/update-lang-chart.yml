name: Update languages chart

on:
  schedule:
    - cron: '0 6 * * *' # daily at 06:00 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install deps
        run: npm ci || npm install

      - name: Generate languages SVG
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run generate-langs

      - name: Commit & push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/langs-donut.svg || true
          if ! git diff --cached --quiet; then
            git commit -m "chore: update languages chart"
            git push
          else
            echo "No changes to commit"
          fi

      - name: Update README with cache-busting URL
        if: success()
        run: |
          TIMESTAMP=$(date +%s)
          for file in README*.md; do
            if [ -f "$file" ]; then
              sed -i "s|assets/langs-donut.svg?cache-bust=[0-9]*|assets/langs-donut.svg?cache-bust=${TIMESTAMP}|" "$file"
              sed -i "s|assets/langs-donut.svg\b|assets/langs-donut.svg?cache-bust=${TIMESTAMP}|" "$file"
              git add "$file"
            fi
          done
          if ! git diff --cached --quiet; then
            git commit -m "chore: update languages chart cache-bust"
            git push
          fi
